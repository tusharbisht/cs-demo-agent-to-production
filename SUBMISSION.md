<!--
  Generated by `cli render-docs` from course.yaml's OpenAPI section.
  DO NOT edit by hand — re-run the renderer to regenerate.
  Source of truth: course.yaml.
-->

# Customer Support Agent
_Version 1.1.0 — API contract for the **Customer Support Agent — Demo to Production** course._
Every test in every `judge.json` must conform to the request body shapes below. These shapes are enforced by `cli validate`; the LLM author is constrained by them at authoring time via tool-use input schemas. **This document is the contract — there is no second source of truth.**

## Endpoints (11)

### `GET /calibration/bin`

**Lookup a single calibration bin**

`operationId`: `getCalibrationBin`

**Responses**:

- `200` — Bin

  ```json
  {}
  ```

---

### `GET /calibration/curve`

**Calibration curve (binned confidence vs accuracy)**

`operationId`: `getCalibrationCurve`

**Responses**:

- `200` — Curve data

  ```json
  {}
  ```

---

### `POST /chat`

**The customer-support endpoint (multi-turn)**

`operationId`: `postChat`

**Request body**:

```json
{
  "conversation_id": "string",
  "user_id": "string",
  "message": "string",
  "stream": false
}
```

| Field | Type | Required | Description |
|---|---|---|---|
| `conversation_id` | `string` | yes |  |
| `user_id` | `string` | yes |  |
| `message` | `string` | yes |  |
| `stream` | `boolean` | no | Optional. When true, return text/event-stream. |

**Responses**:

- `200` — Reply

  ```json
  {
    "conversation_id": "string",
    "reply": "string",
    "citations": [
      {
        "source": "string",
        "quote": "string",
        "char_offset": 0
      }
    ],
    "confidence": 0.0,
    "confidence_reasoning": "string",
    "should_escalate": false,
    "trace_id": "string",
    "model_used": "string",
    "fallback_chain_taken": false,
    "tokens_in": 0,
    "tokens_out": 0,
    "cost_usd": 0.0,
    "latency_ms": 0
  }
  ```
- `503` — Service degraded — all fallback tiers exhausted

  ```json
  {
    "conversation_id": "string",
    "reply": "string",
    "fallback_chain_taken": false,
    "trace_id": "string"
  }
  ```

---

### `GET /eval`

**Eval set + the agent's current scores**

`operationId`: `getEval`

**Responses**:

- `200` — Eval state

  ```json
  {
    "eval_set": [
      {}
    ],
    "scores": {},
    "history": [
      {}
    ]
  }
  ```

---

### `POST /eval/run`

**Trigger an on-demand eval run; returns scores**

`operationId`: `postEvalRun`

**Request body**:

```json
{}
```

**Responses**:

- `200` — Eval results

  ```json
  {}
  ```

---

### `POST /feedback`

**User feedback against a previous reply**

`operationId`: `postFeedback`

**Request body**:

```json
{
  "trace_id": "string",
  "rating": -1,
  "comment": "string",
  "expected_reply": "string",
  "submitted_by": "string"
}
```

| Field | Type | Required | Description |
|---|---|---|---|
| `trace_id` | `string` | yes |  |
| `rating` | `enum(-1, 0, 1)` | yes |  |
| `comment` | `string` | no |  |
| `expected_reply` | `string` | no |  |
| `submitted_by` | `string` | no |  |

**Responses**:

- `200` — Feedback recorded

  ```json
  {
    "ok": false,
    "promoted_to_eval": false
  }
  ```

---

### `GET /feedback/eval-candidates`

**Pending feedback items proposed for eval-set promotion**

`operationId`: `getFeedbackCandidates`

**Responses**:

- `200` — Candidate list

  ```json
  {
    "candidates": [
      {}
    ]
  }
  ```

---

### `GET /health`

**Liveness + readiness probe + which modules are wired**

`operationId`: `getHealth`

**Responses**:

- `200` — Ready

  ```json
  {
    "ok": false,
    "build": "string",
    "model": "string",
    "modules_active": [
      "string"
    ]
  }
  ```

---

### `GET /prompt/versions`

**List versioned prompts the agent knows about**

`operationId`: `getPromptVersions`

**Responses**:

- `200` — Versions

  ```json
  {}
  ```

---

### `GET /state`

**Inspect persisted conversation state**

`operationId`: `getState`

**Responses**:

- `200` — Conversation state

  ```json
  {}
  ```

---

### `GET /trace/{trace_id}`

**Full execution trace for a conversation turn**

`operationId`: `getTrace`

**Responses**:

- `200` — Trace document

  ```json
  {
    "trace_id": "string",
    "conversation_id": "string",
    "user_id": "string",
    "started_at": "string",
    "finished_at": "string",
    "spans": [
      {
        "name": "string",
        "kind": "string",
        "duration_ms": 0
      }
    ],
    "totals": {
      "tokens_in": 0,
      "tokens_out": 0,
      "cost_usd": 0.0,
      "duration_ms": 0
    },
    "feedback": [
      {}
    ],
    "langfuse_url": "string"
  }
  ```

---

